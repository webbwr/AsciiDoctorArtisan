# AsciiDoc Artisan - Production Requirements
# Version: 2.0.5
# Last updated: 2025-11-18
# Python 3.11+ required
#
# These are pinned versions tested and verified for production use.
# For development, use requirements.txt with flexible version ranges.

# ============================================================================
# CORE GUI FRAMEWORK
# ============================================================================
# PySide6 is the official Qt for Python binding
# PySide6-Addons includes QWebEngineView (GPU-accelerated preview rendering)
# PySide6-Essentials includes core Qt modules
# GPU acceleration is automatic with proper graphics drivers installed

PySide6==6.10.0
PySide6-Addons==6.10.0
PySide6-Essentials==6.10.0

# ============================================================================
# DOCUMENT PROCESSING
# ============================================================================

# AsciiDoc Processing
asciidoc3==3.2.3

# Document Conversion (requires Pandoc system binary)
pypandoc==1.15

# PDF Text Extraction (GPU optimized)
pymupdf==1.26.5

# ============================================================================
# SYSTEM INTEGRATION
# ============================================================================

# Secure Credential Storage (OS keyring)
keyring==25.6.0

# System Resource Monitoring
psutil==7.1.3

# Local AI Integration (document conversion)
# Note: Requires Ollama system binary
ollama==0.6.0

# Anthropic Claude AI API integration (v1.10.0+)
# Note: Requires Anthropic API key from console.anthropic.com
anthropic==0.72.0

# Spell checking (v1.8.0+)
pyspellchecker==0.8.1

# ============================================================================
# PERFORMANCE OPTIMIZATION
# ============================================================================

# Faster HTML escaping
MarkupSafe==2.1.3

# Fast fuzzy string matching (v2.0.0+ - auto-complete)
# Rust-based implementation for intelligent completion ranking
rapidfuzz==3.14.0

# Fast non-cryptographic hashing (v1.9.1+ - incremental rendering)
xxhash==3.5.0

# Fast JSON serialization (v1.9.1+ - settings persistence)
orjson==3.10.11

# Qt + asyncio integration (v1.7.0+ - async I/O)
qasync==0.28.0

# Async file operations (v1.6.0+ - non-blocking I/O)
aiofiles==24.1.0

# Data validation and settings (v1.7.0+ - type safety)
pydantic==2.10.5

# Keyring backend for WSL2/Linux without native keyring
keyrings.alt==5.0.2

# ============================================================================
# SYSTEM DEPENDENCIES (Install separately)
# ============================================================================
#
# Required system packages:
# - pandoc (document conversion): sudo apt install pandoc
# - wkhtmltopdf (PDF export): sudo apt install wkhtmltopdf
# - git (version control): sudo apt install git
#
# Optional system packages:
# - ollama (AI features): curl https://ollama.ai/install.sh | sh
#
# GPU/NPU Acceleration (v1.4.0):
# Hardware acceleration is automatic when graphics drivers are installed:
# - NVIDIA: Windows NVIDIA drivers (WSL2 with GPU passthrough)
# - AMD: ROCm drivers (sudo apt install rocm-smi)
# - Intel GPU: Mesa drivers (typically pre-installed)
# - Intel NPU: OpenVINO toolkit (optional, for AI acceleration)
#
# Verify GPU: nvidia-smi (NVIDIA), rocm-smi (AMD), glxinfo (Intel)
#
# ============================================================================
# REMOVED DEPENDENCIES
# ============================================================================
#
# As of v1.4.0, grammar checking has been removed:
# - language-tool-python (removed 2025-10-27)
#
# Reasoning: Grammar functionality caused performance issues and has been
# deprecated in favor of external tools.
#
