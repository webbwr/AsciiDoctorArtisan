# AsciiDoc Artisan Python Dependencies
# Python 3.11+ recommended for best PySide6 compatibility

# Core Qt bindings for GUI
PySide6>=6.9.0

# AsciiDoc rendering engine
asciidoc3

# Document conversion (DOCX, clipboard, etc.)
pypandoc

# Additional recommended packages for complete functionality
# (Add as needed based on adp.py application requirements)
