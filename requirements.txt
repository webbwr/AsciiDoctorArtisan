# AsciiDoc Artisan Python Dependencies
# Version: 1.4.0-beta
# Last updated: 2025-10-27
# Python 3.14+ required

# ============================================================================
# CORE DEPENDENCIES (Required for all installations)
# ============================================================================

# Core Qt bindings for GUI (PySide6 6.9.0+ required for latest features)
PySide6>=6.9.0

# AsciiDoc rendering engine
asciidoc3>=3.2.0

# Document conversion (DOCX, clipboard, etc.)
# Note: Requires Pandoc system binary (install via: sudo apt install pandoc)
pypandoc>=1.13

# PDF text extraction (v1.1 GPU optimization)
# PyMuPDF provides 3-5x faster PDF extraction with GPU acceleration
pymupdf>=1.23.0

# Secure credential storage (OS keyring integration)
keyring>=24.0.0

# System resource monitoring (CPU, memory tracking)
psutil>=5.9.0

# Async file operations (v1.6.0 - non-blocking I/O)
aiofiles>=24.1.0

# Qt + asyncio integration (v1.7.0 Task 4 - Enhanced Async I/O)
qasync>=0.28.0

# Data validation and settings (v1.7.0 - enhanced type safety)
pydantic>=2.0.0

# AI document conversion and chat (Ollama integration v1.2+, chat v1.7.0)
# Note: Requires Ollama system binary for local AI features
# Install: curl https://ollama.ai/install.sh | sh (Linux/Mac)
# Download: https://ollama.ai/download (Windows)
# Models: ollama pull phi3:mini (recommended for AsciiDoc Artisan)
ollama>=0.4.0

# Spell checking (v1.8.0 - integrated spell checker)
# Pure Python spell checker with built-in dictionaries
pyspellchecker>=0.8.0

# ============================================================================
# DEVELOPMENT DEPENDENCIES (Optional - for testing and code quality)
# ============================================================================

# Testing framework
pytest>=7.4.0
pytest-qt>=4.2.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0

# Code formatting and linting
black>=23.0.0
ruff>=0.1.0
isort>=5.12.0

# Type checking
mypy>=1.5.0
types-setuptools>=68.0.0
types-aiofiles>=24.1.0

# Pre-commit hooks
pre-commit>=3.0.0

# ============================================================================
# NOTES
# ============================================================================
#
# GPU/NPU ACCELERATION (v1.4.0):
# Hardware acceleration is built into PySide6 and automatically detected.
# No additional Python packages required. System requirements:
# - NVIDIA GPU: Windows NVIDIA drivers (WSL2 GPU passthrough)
# - AMD GPU: ROCm drivers
# - Intel GPU/NPU: Mesa drivers, OpenVINO (optional)
#
# REMOVED FUNCTIONALITY:
# - Grammar checking (language-tool-python) removed as of v1.4.0
#
# For production use, see requirements-production.txt for pinned versions
#
hypothesis>=6.100.0  # Property-based testing (QA-7)
pytest-benchmark>=5.0.0  # Performance regression testing (QA-8)
pytest-regressions>=2.8.0  # Visual regression testing (QA-9)
numpy>=1.24.0  # Image processing for visual regression tests (QA-9)
